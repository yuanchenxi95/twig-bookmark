export const ROOT = '/';

export const PUBLIC = '/public';

export const LOGIN_PAGE = PUBLIC + '/login';

// ----------------------------------------------------

export const PROTECTED = '/protected';

export const MY_HOME_PAGE = PROTECTED + '/home';
